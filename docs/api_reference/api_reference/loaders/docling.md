=== "pip"

    ```bash
    $ pip install parakeet-index-loaders-docling
    ```

=== "uv"

    ```bash
    $ uv add parakeet-index-loaders-docling
    ```

::: parakeet_index.loaders.docling
    options:
        inherited_members: true
        filters:
        - "!^_"
        - "!class_name"
