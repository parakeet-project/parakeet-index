=== "pip"

    ```bash
    $ pip install parakeet-indexloaders-docling
    ```

=== "uv"

    ```bash
    $ uv add parakeet-indexloaders-docling
    ```

::: parakeet_index.loaders.docling
    options:
        inherited_members: true
        filters:
        - "!^_"
        - "!class_name"
