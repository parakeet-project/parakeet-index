[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "parakeet-index-observability-watsonx"
version = "0.1.0"
description = "parakeet-index observability watsonx integration"
authors = [{ name = "Leonardo Furnielis", email = "leonardofurnielis@outlook.com" }]
license = { text = "Apache-2.0" }
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "certifi>=2026.2.25,<2026.3.0",
    "ipython==9.10.1",
    "ibm-aigov-facts-client>=1.0.105,<1.1.0",
    "ibm-watson-openscale>=3.1.4,<3.2.0",
    "ibm-watsonx-ai>=1.5.5,<1.6.0",
    "parakeet-index-core>=0.1.0,<0.2.0",
]

[tool.hatch.build.targets.sdist]
include = ["parakeet_index"]

[tool.hatch.build.targets.wheel]
include = ["parakeet_index"]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1,<10.0.0",
    "pytest-asyncio>=1.4.0,<2.0.0",
    "ruff>=0.15.20,<0.16.0",
]
