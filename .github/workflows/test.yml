name: Testing

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-instrumentation:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies for parakeet-index-instrumentation
        working-directory: ./parakeet-index-instrumentation
        run: uv sync --extra dev

      - name: Run tests for parakeet-index-instrumentation
        working-directory: ./parakeet-index-instrumentation
        run: uv run pytest -v --tb=short

  test-utils:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies for parakeet-index-utils
        working-directory: ./parakeet-index-utils
        run: uv sync --extra dev

      - name: Run tests for parakeet-index-utils
        working-directory: ./parakeet-index-utils
        run: uv run pytest -v --tb=short

  # test-core:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       python-version: ["3.11", "3.12", "3.13"]
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Install uv
  #       uses: astral-sh/setup-uv@v6

  #     - name: Set up Python ${{ matrix.python-version }}
  #       run: uv python install ${{ matrix.python-version }}

  #     - name: Install dependencies for parakeet-index-core
  #       working-directory: ./parakeet-index-core
  #       run: uv sync --extra dev

  #     - name: Run tests for parakeet-index-core
  #       working-directory: ./parakeet-index-core
  #       run: uv run pytest -v --tb=short
